# CLAUDE.md — docs/1-la-idea/

Reglas locales para cuando edites archivos en esta carpeta. El método completo está en `METODO.md` §1.

## Archivos

Uno por documento vigente: `<slug>.md` en minúsculas-con-guiones. Si el producto tiene módulos independientes: `idea-modulo-a.md`, `idea-modulo-b.md`.

## Regla clave

El documento debe tener **los 12 componentes obligatorios** (10 clásicos + qué NO hace + cómo se mide). La variante de una página vale en fase temprana; la entrega final exige los 12.

## Skill y revisor

- Skill: `documento-de-idea`
- Revisor: `revisor-de-idea` (con `/revisar`)

## Qué NO va aquí

Bloques (van en `docs/2-el-recorrido/`), tareas (en `docs/3-tareas/`), diagramas (en `docs/6-pantallas/`).
